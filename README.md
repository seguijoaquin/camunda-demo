# Camunda Demo APP

## Endpoints

 - `POST /tasks`
 
_Request body_
 
 ```json
{
  "name" : "A not so difficult task",
  "owner" : "Charly",
  "difficulty" : 5
}
```
 - `GET /tasks`
 
 - `GET /tasks/:id`
   
 - `GET /ping`